<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.footballpredictioner">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.chaquo.python.android.PyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FootballPredictioner">
        <activity
            android:name=".PredictingDetailsActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".PredictActivity" />
        <activity
            android:name=".ChosenTeamActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".TableActivity" />
        <activity
            android:name=".ScheduleActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".ChosenLeagueActivity" />
        <activity
            android:name=".PredictActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".ChosenLeagueActivity" />
        <activity
            android:name=".TableActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".ChosenLeagueActivity" />
        <activity
            android:name=".ChosenLeagueActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>